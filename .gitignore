__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
.env
*.egg-info/
# Root-anchored so webui/dist (committed — see bottom) is not ignored
/dist/
/build/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Secrets and local tokens
.env.local

# Downloaded models
models/
*.gguf

# Streamlit / app artifacts
.streamlit/
uploads/
outputs/
*.wav.tmp

# IDE
.idea/
.vscode/
*.swp

terminals/
# Web UI (React/Vite) — dist is committed so runtime needs no Node
webui/node_modules/
.venv310/
